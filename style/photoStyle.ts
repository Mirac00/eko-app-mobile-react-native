import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  photosModalBody: {
    display: 'flex',
    flexDirection: 'row',
    flexWrap: 'wrap',
    justifyContent: 'center',
    alignItems: 'flex-start',
  },
  photoImage: {
    margin: 10, // Adjust margin as per your requirement
  },
});
