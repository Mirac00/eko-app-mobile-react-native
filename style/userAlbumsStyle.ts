// userAlbumsStyle.ts

import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    // inne właściwości stylu dla kontenera
  },
  albumButtons: {
    // inne właściwości stylu dla przycisków albumów
  },
  modalContent: {
    // inne właściwości stylu dla zawartości modala
  },
});

export default styles;
